import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';
import AppBackground from '../components/AppBackground';

const Option = () => {
  return (
    // <AppBackground title={'Setting'}>
    <View style={{flex: 1}}>
      <Text>Hello Options</Text>
    </View>
    // </AppBackground>
  );
};

export default Option;
