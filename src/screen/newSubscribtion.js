import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const newSubscribtion = () => {
  return (
    <View>
      <Text>newSubscribtion</Text>
    </View>
  );
};

export default newSubscribtion;

const styles = StyleSheet.create({});
